# Benchmark Data

This directory contains benchmark profile definitions.

Examples:

- cis_level1.yaml
- cis_level2.yaml

These profiles define security baselines used for misconfiguration detection.

Profiles allow the system to adapt to different compliance levels.
